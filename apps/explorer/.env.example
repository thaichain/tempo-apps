NODE_ENV="development"

# --------------------------------------------------------
# Server runtime
# --------------------------------------------------------

# TIDX basic auth (server-side), format: username:password
TIDX_BASIC_AUTH=""

# TIDX API base URL (hardcoded to https://tidx.thaichain.org in tempo-queries-provider.ts
# for the Thaichain deployment; kept here for reference/local dev only)
TIDX_BASE_URL="https://tidx.thaichain.org"

# RPC auth key (server-side) - not used by Thaichain (direct RPC, no proxy)
TEMPO_RPC_KEY=""

# Datadog browser RUM application ID/client token.
# These stay server-side and are injected into outgoing intake requests by /dd-proxy.
# DATADOG_APPLICATION_ID=""
# DATADOG_CLIENT_TOKEN=""
# DATADOG_SITE="us5.datadoghq.com"

# --------------------------------------------------------
# Client runtime (VITE_ prefix = available in browser)
# --------------------------------------------------------

VITE_BASE_URL=""

# If you need to run explorer against local contract-verification service
# you can set this to localhost
VITE_CONTRACT_VERIFICATION_API_BASE_URL="https://contracts.thaichain.org"

# Datadog browser RUM collection. The browser sends to same-origin /dd-proxy.
# VITE_DATADOG_ENABLED=false
# VITE_DATADOG_SERVICE="explorer"
# VITE_DATADOG_ENV="development"
# VITE_DATADOG_SESSION_SAMPLE_RATE=100
# VITE_DATADOG_SESSION_REPLAY_SAMPLE_RATE=0
# VITE_DATADOG_TRACE_SAMPLE_RATE=20
# VITE_DATADOG_ALLOWED_TRACING_URLS=""

# --------------------------------------------------------
# Sentry (server + client)
# --------------------------------------------------------
# SENTRY_DSN=""
# SENTRY_AUTH_TOKEN=""
# SENTRY_ORG=""
# SENTRY_PROJECT=""
# SENTRY_TRACES_SAMPLE_RATE="0.1"
# VITE_SENTRY_DSN=""
# VITE_SENTRY_TRACES_SAMPLE_RATE="0.1"

# --------------------------------------------------------
# Build-time only
# --------------------------------------------------------
# PORT=3007
# CLOUDFLARE_ENV="testnet"
# ANALYZE=false
# ANALYZE_JSON=false
# CF_PAGES_COMMIT_SHA=""

# --------------------------------------------------------
# The following vars are set automatically per environment
# in wrangler.json and are generally not to be set here.
# --------------------------------------------------------
# VITE_TEMPO_ENV="testnet"  # devnet | nextfork | mainnet | testnet | thaichain

# Show react query + react router devtools
# VITE_ENABLE_DEVTOOLS=true

# (dev/preview) comma-separated list of allowed hosts to use in vite config. See
# https://vite.dev/config/server-options
# https://vite.dev/config/preview-options#preview-allowedhosts
# ALLOWED_HOSTS=""
